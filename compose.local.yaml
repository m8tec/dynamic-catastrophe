services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: dynamic-catastrophe:local
    container_name: dynamic-catastrophe
    restart: unless-stopped
    ports:
      - "${APP_PORT:-3000}:3000"
    environment:
      NODE_ENV: production
      NEXT_TELEMETRY_DISABLED: "1"

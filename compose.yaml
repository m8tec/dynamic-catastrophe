services:
  app:
    image: ghcr.io/m8tec/dynamic-catastrophe:latest
    container_name: dynamic-catastrophe
    restart: unless-stopped
    ports:
      - "${APP_PORT:-3000}:3000"
    environment:
      NODE_ENV: production
      NEXT_TELEMETRY_DISABLED: "1"
